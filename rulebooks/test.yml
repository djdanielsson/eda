---
- name: Capture POSTs
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Launch Demo Job
      condition: event.payload is defined
      action:
        run_job_template:
          name: Demo Job Template
          organization: Default

    - name: Test
      condition: event.payload is defined
      action:
        debug:
...
